<Window
    x:Class="TurtleClass.WinUI.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="TurtleClass">
    <Grid Padding="24" RowDefinitions="Auto,*,Auto" RowSpacing="16">
        <StackPanel Spacing="4">
            <TextBlock Text="TurtleClass" FontSize="28" FontWeight="SemiBold" />
            <TextBlock Text="Offline-first classroom points client" Opacity="0.72" />
        </StackPanel>

        <Grid Grid.Row="1" ColumnDefinitions="2*,3*" ColumnSpacing="16">
            <ListView x:Name="StudentsList" Header="Students" SelectionMode="Single" />
            <StackPanel Grid.Column="1" Spacing="12">
                <TextBlock Text="Quick points" FontSize="20" FontWeight="SemiBold" />
                <NumberBox x:Name="PointsDeltaBox" Header="Points delta" Value="1" SpinButtonPlacementMode="Inline" />
                <Button x:Name="QueuePointsButton" Content="Queue offline event" Click="QueuePointsButton_Click" />
                <InfoBar x:Name="SyncStatusBar" Title="Offline queue ready" Message="Events are queued locally until sync protocol v1 is connected." IsOpen="True" Severity="Informational" />
            </StackPanel>
        </Grid>

        <TextBlock Grid.Row="2" Text="UI shell only: business state is projected by Core; this view only queues command intent." Opacity="0.64" />
    </Grid>
</Window>
